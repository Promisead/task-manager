# MongoDB connection string (replace <username>, <password>, and <dbname> with your own)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.cm0hr.mongodb.net/<dbname>?retryWrites=true&w=majority

# Port number for the backend server
PORT=5000